{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "http://localhost:8080/json-schema/getOneFilenameMetadataStrategySchema.json",
	"$base": "https://localhost:8080/api/",
	"title": "The filename metadata strategy schema",
	"description": "Schema representing the filename metadata extraction strategy.",
	"type": "object",
	"required": [
		"data"
	],
	"additionalProperties": false,
	"properties": {
		"data": {
			"type": "object",
			"required": [
				"id",
				"type",
				"attributes"
			],
			"properties": {
				"id": {
					"type": "integer",
					"description": "Identifier of the strategy. Server generated."
				},
				"type": {
					"const": "strategy",
					"description": "The strategy used to parse a file name."
				},
				"attributes": {
					"required" : [
						"strategyName",
						"regex",
						"targets"
					],
					"properties": {
						"strategyName": {
							"type": "string",
							"description": "Name of the strategy."
						},
						"regex": {
							"type": "string",
							"description": "Pattern as Regex used to parse file name."
						},
						"targets": {
							"type": "array",
							"items": {
								"type": "object",
								"description": "List of key/value pairs matching the capture group from the regex to a managed attribute."
							}
						}
					}
				}
			}
		}
	}
}
