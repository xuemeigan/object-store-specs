{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "http://localhost:8080/json-schema/postFilenameMetadataRequestSchema.json",
	"$base": "https://localhost:8080/api/",
	"title" : "The filename metadata request schema",
	"description": "Schema representing the request to extract metadata from a filename and a strategy.",
	"type": "object",
	"required": [
		"data"
	],
	"properties": {
		"data": {
			"type": "object",
			"properties": {
				"attributes": {
					"required" : [
						"fileName",
						"strategyId"
					],
					"properties": {
						"fileName": {
							"type": "string",
							"description": "The file name to be parsed."
						},
						"strategyId": {
							"type": "integer",
							"description": "Identifier of the strategy being used to parse the filename."
						}
					}
				}
			}
		}
	}
}
